import { Type } from 'class-transformer';
import { REGEX_ID, REGEX_BASEFORMULAPRODUCTNAME, REGEX_ADDITIVEPRODUCTNAME, REGEX_ADMINISTRATIONINSTRUCTION } from '../FhirTsLibValidation/FhirRegex';
import { CodeableConcept, Element, Extension, NutritionOrderAdministration, Quantity } from './index';



/**
 * A request to supply a diet, formula feeding (enteral) or oral nutritional supplement to a
 * patient/resident.
 *
 * Generated by Gallaware, Inc. from:
 *    file:/Users/admin/Documents/DevNetbeans/jsonSchemaToJava/target/classes/schema/fhir.schema.json#/definitions/NutritionOrder_EnteralFormula
 */
export class NutritionOrderEnteralFormula {
  /**
   * A sequence of Unicode characters
   * Field access restricted because there are specific valid values.
   */
  protected id?: string;

  /**
   * May be used to represent additional information that is not part of the basic definition of the
   * element. To make the use of extensions safe and manageable, there is a strict set of governance
   * applied to the definition and use of extensions. Though any implementer can define an extension,
   * there is a set of requirements that SHALL be met as part of the definition of the extension.
   * Field access restricted because there are specific valid values.
   */
  @Type(() => Extension)
  protected extension?: Extension[];

  /**
   * May be used to represent additional information that is not part of the basic definition of the
   * element and that modifies the understanding of the element in which it is contained and/or the
   * understanding of the containing element's descendants. Usually modifier elements provide negation
   * or qualification. To make the use of extensions safe and manageable, there is a strict set of
   * governance applied to the definition and use of extensions. Though any implementer can define an
   * extension, there is a set of requirements that SHALL be met as part of the definition of the
   * extension. Applications processing a resource are required to check for modifier extensions.
   * 
   * Modifier extensions SHALL NOT change the meaning of any elements on Resource or DomainResource
   * (including cannot change the meaning of modifierExtension itself).
   * Field access restricted because there are specific valid values.
   */
  @Type(() => Extension)
  protected modifierExtension?: Extension[];

  /**
   * A concept that may be defined by a formal reference to a terminology or ontology or may be provided
   * by text.
   */
  @Type(() => CodeableConcept)
   baseFormulaType?: CodeableConcept;

  /**
   * A sequence of Unicode characters
   * Field access restricted because there are specific valid values.
   */
  protected baseFormulaProductName?: string;

  /**
   * Base definition for all elements in a resource.
   */
  @Type(() => Element)
   _baseFormulaProductName?: Element;

  /**
   * A concept that may be defined by a formal reference to a terminology or ontology or may be provided
   * by text.
   */
  @Type(() => CodeableConcept)
   additiveType?: CodeableConcept;

  /**
   * A sequence of Unicode characters
   * Field access restricted because there are specific valid values.
   */
  protected additiveProductName?: string;

  /**
   * Base definition for all elements in a resource.
   */
  @Type(() => Element)
   _additiveProductName?: Element;

  /**
   * A measured amount (or an amount that can potentially be measured). Note that measured amounts
   * include amounts that are not precisely quantified, including amounts involving arbitrary units and
   * floating currencies.
   */
  @Type(() => Quantity)
   caloricDensity?: Quantity;

  /**
   * A concept that may be defined by a formal reference to a terminology or ontology or may be provided
   * by text.
   */
  @Type(() => CodeableConcept)
   routeofAdministration?: CodeableConcept;

  /**
   * Formula administration instructions as structured data.  This repeating structure allows for
   * changing the administration rate or volume over time for both bolus and continuous feeding.  An
   * example of this would be an instruction to increase the rate of continuous feeding every 2 hours.
   * Field access restricted because there are specific valid values.
   */
  @Type(() => NutritionOrderAdministration)
  protected administration?: NutritionOrderAdministration[];

  /**
   * A measured amount (or an amount that can potentially be measured). Note that measured amounts
   * include amounts that are not precisely quantified, including amounts involving arbitrary units and
   * floating currencies.
   */
  @Type(() => Quantity)
   maxVolumeToDeliver?: Quantity;

  /**
   * A sequence of Unicode characters
   * Field access restricted because there are specific valid values.
   */
  protected administrationInstruction?: string;

  /**
   * Base definition for all elements in a resource.
   */
  @Type(() => Element)
   _administrationInstruction?: Element;


  /*
  * This sets the data ensuring that the value is valid pattern.  If not valid, it throws an error
  * that should be caught.
  */
  public setId(newValue: string) {
    if (typeof(newValue) === 'undefined') {
      if (typeof(this.id) !== 'undefined') {
         delete this.id;
      }
      return;
    }
    
    if (REGEX_ID.test(newValue)) {
      this.id = newValue
    } else {
      throw new Error("Invalid value pattern for id of " + newValue);
    }
  }
  
  public getIdPattern(): string {
    return REGEX_ID.source;
  }
  
  /**
  * Gets the current value of field id.
  * @returns the value  or undefined
  */
  public getId(): string | undefined {
      return this.id;
  }

  /**
  * Gets the current value of field id if it exists,
  * but if it is 'undefined' it will return the value supplied
  * as the parameter.
  * @returns the value or the parameter value
  */
  public getIdOrElse(defaultValue: string): string {
      if (this.id == undefined || this.id == null ) { return defaultValue; }
      return this.id;
  }

  public addExtension(newValue: Extension) {
    if (typeof(newValue) === 'undefined') {
      if (typeof(this.extension) !== 'undefined') {
         delete this.extension;
      }
      return;
    }
    
    if (typeof(this.extension) === 'undefined') {
      this.extension =  [];
    }
    this.extension.push(newValue);
  }
  /**
  * Removes all elements from the array extension, returning the deleted elements.
  * @returns An array containing the elements that were deleted.
  */
  public deleteExtensionAll(): Extension[] {
    if (typeof(this.extension) !== 'undefined') {
       const oldArray = this.extension;
       delete this.extension;
       return oldArray;
    }
    return [];
  }
  
  /**
  * Removes elements from the array extension, returning the deleted elements.
  * @param start The zero-based location in the array from which to start removing elements.
  * @param deleteCount The number of elements to remove.
  * @returns An array containing the elements that were deleted, or an empty array if the element
  *     was undefined.
  */
  public deleteExtensionBySplice(start: number, deleteCount?: number): Extension[] {
    if (typeof(this.extension) !== 'undefined') {
       return this.extension.splice(start, deleteCount);
    }
    return [];
  }
  
  /**
  * Gets the current value of field extension.
  * @returns the value  or undefined
  */
  public getExtension(): Extension[] | undefined {
      return this.extension;
  }

  /**
  * Gets the current value of field extension if it exists,
  * but if it is 'undefined' it will return the value supplied
  * as the parameter.
  * @returns the value or the parameter value
  */
  public getExtensionOrElse(defaultValue: Extension[]): Extension[] {
      if (this.extension == undefined || this.extension == null ) { return defaultValue; }
      return this.extension;
  }

  public addModifierExtension(newValue: Extension) {
    if (typeof(newValue) === 'undefined') {
      if (typeof(this.modifierExtension) !== 'undefined') {
         delete this.modifierExtension;
      }
      return;
    }
    
    if (typeof(this.modifierExtension) === 'undefined') {
      this.modifierExtension =  [];
    }
    this.modifierExtension.push(newValue);
  }
  /**
  * Removes all elements from the array modifierExtension, returning the deleted elements.
  * @returns An array containing the elements that were deleted.
  */
  public deleteModifierExtensionAll(): Extension[] {
    if (typeof(this.modifierExtension) !== 'undefined') {
       const oldArray = this.modifierExtension;
       delete this.modifierExtension;
       return oldArray;
    }
    return [];
  }
  
  /**
  * Removes elements from the array modifierExtension, returning the deleted elements.
  * @param start The zero-based location in the array from which to start removing elements.
  * @param deleteCount The number of elements to remove.
  * @returns An array containing the elements that were deleted, or an empty array if the element
  *     was undefined.
  */
  public deleteModifierExtensionBySplice(start: number, deleteCount?: number): Extension[] {
    if (typeof(this.modifierExtension) !== 'undefined') {
       return this.modifierExtension.splice(start, deleteCount);
    }
    return [];
  }
  
  /**
  * Gets the current value of field modifierExtension.
  * @returns the value  or undefined
  */
  public getModifierExtension(): Extension[] | undefined {
      return this.modifierExtension;
  }

  /**
  * Gets the current value of field modifierExtension if it exists,
  * but if it is 'undefined' it will return the value supplied
  * as the parameter.
  * @returns the value or the parameter value
  */
  public getModifierExtensionOrElse(defaultValue: Extension[]): Extension[] {
      if (this.modifierExtension == undefined || this.modifierExtension == null ) { return defaultValue; }
      return this.modifierExtension;
  }

  /*
  * This sets the data ensuring that the value is valid pattern.  If not valid, it throws an error
  * that should be caught.
  */
  public setBaseFormulaProductName(newValue: string) {
    if (typeof(newValue) === 'undefined') {
      if (typeof(this.baseFormulaProductName) !== 'undefined') {
         delete this.baseFormulaProductName;
      }
      return;
    }
    
    if (REGEX_BASEFORMULAPRODUCTNAME.test(newValue)) {
      this.baseFormulaProductName = newValue
    } else {
      throw new Error("Invalid value pattern for baseFormulaProductName of " + newValue);
    }
  }
  
  public getBaseFormulaProductNamePattern(): string {
    return REGEX_BASEFORMULAPRODUCTNAME.source;
  }
  
  /**
  * Gets the current value of field baseFormulaProductName.
  * @returns the value  or undefined
  */
  public getBaseFormulaProductName(): string | undefined {
      return this.baseFormulaProductName;
  }

  /**
  * Gets the current value of field baseFormulaProductName if it exists,
  * but if it is 'undefined' it will return the value supplied
  * as the parameter.
  * @returns the value or the parameter value
  */
  public getBaseFormulaProductNameOrElse(defaultValue: string): string {
      if (this.baseFormulaProductName == undefined || this.baseFormulaProductName == null ) { return defaultValue; }
      return this.baseFormulaProductName;
  }

  /*
  * This sets the data ensuring that the value is valid pattern.  If not valid, it throws an error
  * that should be caught.
  */
  public setAdditiveProductName(newValue: string) {
    if (typeof(newValue) === 'undefined') {
      if (typeof(this.additiveProductName) !== 'undefined') {
         delete this.additiveProductName;
      }
      return;
    }
    
    if (REGEX_ADDITIVEPRODUCTNAME.test(newValue)) {
      this.additiveProductName = newValue
    } else {
      throw new Error("Invalid value pattern for additiveProductName of " + newValue);
    }
  }
  
  public getAdditiveProductNamePattern(): string {
    return REGEX_ADDITIVEPRODUCTNAME.source;
  }
  
  /**
  * Gets the current value of field additiveProductName.
  * @returns the value  or undefined
  */
  public getAdditiveProductName(): string | undefined {
      return this.additiveProductName;
  }

  /**
  * Gets the current value of field additiveProductName if it exists,
  * but if it is 'undefined' it will return the value supplied
  * as the parameter.
  * @returns the value or the parameter value
  */
  public getAdditiveProductNameOrElse(defaultValue: string): string {
      if (this.additiveProductName == undefined || this.additiveProductName == null ) { return defaultValue; }
      return this.additiveProductName;
  }

  public addAdministration(newValue: NutritionOrderAdministration) {
    if (typeof(newValue) === 'undefined') {
      if (typeof(this.administration) !== 'undefined') {
         delete this.administration;
      }
      return;
    }
    
    if (typeof(this.administration) === 'undefined') {
      this.administration =  [];
    }
    this.administration.push(newValue);
  }
  /**
  * Removes all elements from the array administration, returning the deleted elements.
  * @returns An array containing the elements that were deleted.
  */
  public deleteAdministrationAll(): NutritionOrderAdministration[] {
    if (typeof(this.administration) !== 'undefined') {
       const oldArray = this.administration;
       delete this.administration;
       return oldArray;
    }
    return [];
  }
  
  /**
  * Removes elements from the array administration, returning the deleted elements.
  * @param start The zero-based location in the array from which to start removing elements.
  * @param deleteCount The number of elements to remove.
  * @returns An array containing the elements that were deleted, or an empty array if the element
  *     was undefined.
  */
  public deleteAdministrationBySplice(start: number, deleteCount?: number): NutritionOrderAdministration[] {
    if (typeof(this.administration) !== 'undefined') {
       return this.administration.splice(start, deleteCount);
    }
    return [];
  }
  
  /**
  * Gets the current value of field administration.
  * @returns the value  or undefined
  */
  public getAdministration(): NutritionOrderAdministration[] | undefined {
      return this.administration;
  }

  /**
  * Gets the current value of field administration if it exists,
  * but if it is 'undefined' it will return the value supplied
  * as the parameter.
  * @returns the value or the parameter value
  */
  public getAdministrationOrElse(defaultValue: NutritionOrderAdministration[]): NutritionOrderAdministration[] {
      if (this.administration == undefined || this.administration == null ) { return defaultValue; }
      return this.administration;
  }

  /*
  * This sets the data ensuring that the value is valid pattern.  If not valid, it throws an error
  * that should be caught.
  */
  public setAdministrationInstruction(newValue: string) {
    if (typeof(newValue) === 'undefined') {
      if (typeof(this.administrationInstruction) !== 'undefined') {
         delete this.administrationInstruction;
      }
      return;
    }
    
    if (REGEX_ADMINISTRATIONINSTRUCTION.test(newValue)) {
      this.administrationInstruction = newValue
    } else {
      throw new Error("Invalid value pattern for administrationInstruction of " + newValue);
    }
  }
  
  public getAdministrationInstructionPattern(): string {
    return REGEX_ADMINISTRATIONINSTRUCTION.source;
  }
  
  /**
  * Gets the current value of field administrationInstruction.
  * @returns the value  or undefined
  */
  public getAdministrationInstruction(): string | undefined {
      return this.administrationInstruction;
  }

  /**
  * Gets the current value of field administrationInstruction if it exists,
  * but if it is 'undefined' it will return the value supplied
  * as the parameter.
  * @returns the value or the parameter value
  */
  public getAdministrationInstructionOrElse(defaultValue: string): string {
      if (this.administrationInstruction == undefined || this.administrationInstruction == null ) { return defaultValue; }
      return this.administrationInstruction;
  }


}


