import { getValidValueIdx } from '../FhirTsLibValidation/FhirUtils';
import { Type } from 'class-transformer';
import { REGEX_ID, REGEX_ENDPOINT, REGEX_PAYLOAD } from '../FhirTsLibValidation/FhirRegex';
import { Element, Extension } from './FhirType';
import { SubscriptionChannelType_list_ValidValues } from '../FhirTsLibValidation/Fhir_ValidValues';



/**
 * The subscription resource is used to define a push-based subscription from a server to another
 * system. Once a subscription is registered with the server, the server checks every resource that
 * is created or updated, and if the resource matches the given criteria, it sends a message on the
 * defined "channel" so that another system can take an appropriate action.
 *
 * Generated by Gallaware, Inc. from:
 *    file:/Users/admin/Documents/DevNetbeans/jsonSchemaToJava/target/classes/schema/fhir.schema.json#/definitions/Subscription_Channel
 */
export class SubscriptionChannel {
  /**
   * A sequence of Unicode characters
   * Field access restricted because there are specific valid values.
   */
  protected id?: string;

  /**
   * May be used to represent additional information that is not part of the basic definition of the
   * element. To make the use of extensions safe and manageable, there is a strict set of governance
   * applied to the definition and use of extensions. Though any implementer can define an extension,
   * there is a set of requirements that SHALL be met as part of the definition of the extension.
   * Field access restricted because there are specific valid values.
   */
  @Type(() => Extension)
  protected extension?: Extension[];

  /**
   * May be used to represent additional information that is not part of the basic definition of the
   * element and that modifies the understanding of the element in which it is contained and/or the
   * understanding of the containing element's descendants. Usually modifier elements provide negation
   * or qualification. To make the use of extensions safe and manageable, there is a strict set of
   * governance applied to the definition and use of extensions. Though any implementer can define an
   * extension, there is a set of requirements that SHALL be met as part of the definition of the
   * extension. Applications processing a resource are required to check for modifier extensions.
   * 
   * Modifier extensions SHALL NOT change the meaning of any elements on Resource or DomainResource
   * (including cannot change the meaning of modifierExtension itself).
   * Field access restricted because there are specific valid values.
   */
  @Type(() => Extension)
  protected modifierExtension?: Extension[];

  /**
   * The type of channel to send notifications on.
   * Field access restricted because there are specific valid values.
   */
  protected type?: string;

  /**
   * Base definition for all elements in a resource.
   */
  @Type(() => Element)
   _type?: Element;

  /**
   * A URI that is a literal reference
   * Field access restricted because there are specific valid values.
   */
  protected endpoint?: string;

  /**
   * Base definition for all elements in a resource.
   */
  @Type(() => Element)
   _endpoint?: Element;

  /**
   * A string which has at least one character and no leading or trailing whitespace and where there is
   * no whitespace other than single spaces in the contents
   * Field access restricted because there are specific valid values.
   */
  protected payload?: string;

  /**
   * Base definition for all elements in a resource.
   */
  @Type(() => Element)
   _payload?: Element;

  /**
   * Additional headers / information to send as part of the notification.
   * Field access restricted because there are specific valid values.
   */
  protected header?: string[];

  /**
   * Extensions for header
   * Field access restricted because there are specific valid values.
   */
  @Type(() => Element)
  protected _header?: Element[];


  /*
  * This sets the data ensuring that the value is valid pattern.  If not valid, it throws an error
  * that should be caught.
  */
  public setId(newValue: string) {
    if (typeof(newValue) === 'undefined') {
      if (typeof(this.id) !== 'undefined') {
         delete this.id;
      }
      return;
    }
    
    if (REGEX_ID.test(newValue)) {
      this.id = newValue
    } else {
      throw new Error("Invalid value pattern for id of " + newValue);
    }
  }
  
  public getIdPattern(): string {
    return REGEX_ID.source;
  }
  

  public getId(): string | undefined {
      return this.id;
  }

  public addExtension(newValue: Extension) {
    if (typeof(newValue) === 'undefined') {
      if (typeof(this.extension) !== 'undefined') {
         delete this.extension;
      }
      return;
    }
    
    if (typeof(this.extension) === 'undefined') {
      this.extension =  [];
    }
    this.extension.push(newValue);
  }

  public getExtension(): Extension[] | undefined {
      return this.extension;
  }

  public addModifierExtension(newValue: Extension) {
    if (typeof(newValue) === 'undefined') {
      if (typeof(this.modifierExtension) !== 'undefined') {
         delete this.modifierExtension;
      }
      return;
    }
    
    if (typeof(this.modifierExtension) === 'undefined') {
      this.modifierExtension =  [];
    }
    this.modifierExtension.push(newValue);
  }

  public getModifierExtension(): Extension[] | undefined {
      return this.modifierExtension;
  }

  /*
  * This sets the data ensuring that the value is valid.  If not valid, it throws an error
  * that should be caught.
  */
  public setType(newValue: string) {
    if (typeof(newValue) === 'undefined') {
      if (typeof(this.type) !== 'undefined') {
         delete this.type;
      }
      return;
    }
    
    const idx = getValidValueIdx(SubscriptionChannelType_list_ValidValues, newValue);
    if (idx >= 0) {
      this.type = SubscriptionChannelType_list_ValidValues[idx];
    } else {
      throw new Error("Unknown type of " + newValue);
    }
  }
  
  public getSubscriptionChannelTypeValidValues(): string[] {
    return SubscriptionChannelType_list_ValidValues;
  }
  

  public getType(): string | undefined {
      return this.type;
  }

  /*
  * This sets the data ensuring that the value is valid pattern.  If not valid, it throws an error
  * that should be caught.
  */
  public setEndpoint(newValue: string) {
    if (typeof(newValue) === 'undefined') {
      if (typeof(this.endpoint) !== 'undefined') {
         delete this.endpoint;
      }
      return;
    }
    
    if (REGEX_ENDPOINT.test(newValue)) {
      this.endpoint = newValue
    } else {
      throw new Error("Invalid value pattern for endpoint of " + newValue);
    }
  }
  
  public getEndpointPattern(): string {
    return REGEX_ENDPOINT.source;
  }
  

  public getEndpoint(): string | undefined {
      return this.endpoint;
  }

  /*
  * This sets the data ensuring that the value is valid pattern.  If not valid, it throws an error
  * that should be caught.
  */
  public setPayload(newValue: string) {
    if (typeof(newValue) === 'undefined') {
      if (typeof(this.payload) !== 'undefined') {
         delete this.payload;
      }
      return;
    }
    
    if (REGEX_PAYLOAD.test(newValue)) {
      this.payload = newValue
    } else {
      throw new Error("Invalid value pattern for payload of " + newValue);
    }
  }
  
  public getPayloadPattern(): string {
    return REGEX_PAYLOAD.source;
  }
  

  public getPayload(): string | undefined {
      return this.payload;
  }

  public addHeader(newValue: string) {
    if (typeof(newValue) === 'undefined') {
      if (typeof(this.header) !== 'undefined') {
         delete this.header;
      }
      return;
    }
    
    if (typeof(this.header) === 'undefined') {
      this.header =  [];
    }
    this.header.push(newValue);
  }

  public getHeader(): string[] | undefined {
      return this.header;
  }

  public add_header(newValue: Element) {
    if (typeof(newValue) === 'undefined') {
      if (typeof(this._header) !== 'undefined') {
         delete this._header;
      }
      return;
    }
    
    if (typeof(this._header) === 'undefined') {
      this._header =  [];
    }
    this._header.push(newValue);
  }

  public get_header(): Element[] | undefined {
      return this._header;
  }


}


